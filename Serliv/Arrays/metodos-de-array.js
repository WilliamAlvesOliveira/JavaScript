/*===============================================
    Métodos principais de Array em JavaScript
 ==============================================*/

 
 * - concat(array1, array2, ...): retorna um novo array que é a concatenação dos arrays passados.
 * - slice(inicio, fim): retorna um novo array contendo uma cópia parcial do original (do índice inicio até fim - 1).
 * - splice(indice, quantidade, elementos...): altera o array original removendo e/ou adicionando elementos.
 * - forEach(callback): executa a função callback para cada elemento do array (sem retornar nada).
 * - map(callback): retorna um novo array contendo o resultado da aplicação do callback em cada elemento.
 * - filter(callback): retorna um novo array com os elementos que passaram no teste do callback.
 * - reduce(callback, valorInicial): reduz o array a um único valor, acumulando resultados via callback.
 * - find(callback): retorna o primeiro elemento que satisfaz o teste do callback, ou undefined.
 * - findIndex(callback): retorna o índice do primeiro elemento que satisfaz o teste, ou -1.
 * - some(callback): retorna true se algum elemento satisfaz o teste.
 * - every(callback): retorna true se todos os elementos satisfazem o teste.
 * - includes(valor): retorna true se o array contém o valor especificado.
 * - indexOf(valor): retorna o índice da primeira ocorrência do valor ou -1.
 * - lastIndexOf(valor): retorna o índice da última ocorrência do valor ou -1.
 * - sort(callback): ordena o array no lugar, opcionalmente usando a função de comparação.
 * - reverse(): inverte a ordem dos elementos do array no lugar.
 * - join(separador): une os elementos do array em uma string, separados pelo separador.
 * - fill(valor, inicio, fim): preenche os elementos do array com o valor entre os índices inicio e fim.
 * - flat(profundidade): retorna um novo array com os subarrays "achatados" até a profundidade especificada.
 * - flatMap(callback): mapeia e achata o array em uma única operação.
 

